import React from 'react';
import './index.css';

class Landing extends React.Component {
    state = {

    };

    render() {
        return (
            <div className={'landing-container'}>{'TEST TEXT LANDING'}</div>
        );
    }
}

export default Landing;
